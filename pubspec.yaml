name: flutter_mobx_structure_validator
description: A plugin for checking MobX projects structure
version: 1.0.0

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
